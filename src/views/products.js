import { getSupabase, supaCall, logActivity, getCurrentUserId } from '../utils/supabase-client.js';import { sanitize, formatCurrency, showToast, exportToCSV } from '../utils/helpers.js';import { t } from '../config/i18n.js';import { navigateTo, goBack } from '../navigation/router.js';const tg=window.tg;
export async function showProducts(){window.currentRefreshFunction=showProducts;tg.MainButton.hide();try{const{data:products}=await supaCall(()=>getSupabase().from('products').select('id, name, variants(id, variant_name, purchase_price, selling_price, quantity, min_quantity)').order('name'));let html=`<div class="card"><h2>${t('products')}</h2><input class="search-input" id="search-products" placeholder="${t('search')}"/><div id="products-list">`;if(!products?.length)html+=`<div class="empty-state">📦<br>${t('noData')}</div>`;else products.forEach(p=>{html+=`<div class="product-card"><strong>${sanitize(p.name)}</strong> <button class="btn btn-sm btn-outline" onclick="navigateTo('edit-product',{productId:${p.id}})">${t('edit')}</button> <button class="btn btn-sm btn-danger" onclick="window.deleteProduct(${p.id})">${t('delete')}</button><ul>`;p.variants?.forEach(v=>{const warn=(v.min_quantity>0&&v.quantity<=v.min_quantity)?' ⚠️':'';html+=`<li>${sanitize(v.variant_name||'غير مسمى')} - شراء: ${formatCurrency(v.purchase_price)} / بيع: ${formatCurrency(v.selling_price)} (${v.quantity}) ${t('minAlert')}: ${v.min_quantity}${warn} <button class="btn btn-sm btn-danger" onclick="window.deleteVariant(${v.id})">${t('delete')}</button></li>`;});html+='</ul></div>';});html+=`</div><button class="btn" id="add-product-btn">+ ${t('addProduct')}</button> <button class="btn btn-outline" id="export-stock-btn">📥 ${t('exportCSV')}</button></div>`;document.getElementById('view').innerHTML=html;document.getElementById('search-products').addEventListener('input',e=>{const term=e.target.value.toLowerCase();document.querySelectorAll('.product-card').forEach(card=>card.style.display=card.textContent.toLowerCase().includes(term)?'':'none');});document.getElementById('add-product-btn').addEventListener('click',()=>navigateTo('add-product'));document.getElementById('export-stock-btn').addEventListener('click',exportStockCSV);}catch(e){document.getElementById('view').innerHTML=`<div class="card" style="background:#fff3cd;color:#856404;padding:20px;"><strong>⚠️ خطأ في تحميل الأصناف</strong><br>${e.message}</div>`;}}
window.deleteProduct=async id=>{if(!confirm(t('confirmDelete')))return;try{await supaCall(()=>getSupabase().from('products').delete().eq('id',id));await logActivity('delete_product',`ID:${id}`);showToast(t('deleted'));showProducts();}catch(err){showToast(err.message,true);}};
window.deleteVariant=async id=>{if(!confirm(t('confirmDelete')))return;try{await supaCall(()=>getSupabase().from('variants').delete().eq('id',id));await logActivity('delete_variant',`ID:${id}`);showToast(t('deleted'));showProducts();}catch(err){showToast(err.message,true);}};
async function exportStockCSV(){const{data:products}=await supaCall(()=>getSupabase().from('products').select('name, variants(variant_name, purchase_price, selling_price, quantity, min_quantity)'));const headers=[t('products'),'المتغير','سعر الشراء','سعر البيع',t('quantity'),t('minAlert')];const rows=[];products.forEach(p=>p.variants?.forEach(v=>rows.push([p.name,v.variant_name||'',v.purchase_price,v.selling_price,v.quantity,v.min_quantity])));exportToCSV('المخزون.csv',headers,rows);}
export async function showAddProductForm(){tg.BackButton.show();tg.MainButton.setText(t('save'));tg.MainButton.show();tg.MainButton.onClick(saveProductWithVariants);document.getElementById('view').innerHTML=`<div class="card"><h3>${t('addProduct')}</h3><input id="pname" placeholder="اسم الصنف"/><div id="variants-container"></div><button class="btn btn-outline" id="add-variant-row">+ إضافة متغير</button></div>`;document.getElementById('add-variant-row').addEventListener('click',addVariantRow);addVariantRow();}
function addVariantRow(){const row=document.createElement('div');row.className='variant-row';row.innerHTML=`<input placeholder="اسم المتغير" class="v-name"/><input type="number" placeholder="سعر الشراء" class="v-purchase-price"/><input type="number" placeholder="سعر البيع" class="v-selling-price"/><input type="number" placeholder="الكمية" class="v-qty"/><input type="number" placeholder="حد التنبيه" class="v-min" value="0"/><button class="btn btn-sm btn-danger remove-variant">X</button>`;row.querySelector('.remove-variant').addEventListener('click',()=>row.remove());document.getElementById('variants-container').appendChild(row);}
async function saveProductWithVariants(){const name=document.getElementById('pname').value.trim();if(!name){showToast(t('fillAllFields'),true);return;}const rows=document.querySelectorAll('.variant-row');const variants=[];let hasError=false;rows.forEach(row=>{const vname=row.querySelector('.v-name').value.trim()||'بدون اسم';const purchasePrice=parseFloat(row.querySelector('.v-purchase-price').value);const sellingPrice=parseFloat(row.querySelector('.v-selling-price').value);const qty=parseInt(row.querySelector('.v-qty').value);const minQty=parseInt(row.querySelector('.v-min').value)||0;if(isNaN(purchasePrice)||purchasePrice<0||isNaN(sellingPrice)||sellingPrice<0||isNaN(qty)||qty<0){hasError=true;return;}variants.push({variant_name:vname,purchase_price:purchasePrice,selling_price:sellingPrice,quantity:qty,min_quantity:minQty});});if(hasError||!variants.length){showToast(t('fillAllFields'),true);return;}tg.MainButton.disable();tg.MainButton.showProgress();try{const{data:product,error}=await supaCall(()=>getSupabase().from('products').insert({name,user_id:getCurrentUserId()}).select().single());if(error)throw error;const varsData=variants.map(v=>({...v,product_id:product.id,user_id:getCurrentUserId()}));await supaCall(()=>getSupabase().from('variants').insert(varsData));await logActivity('add_product',`Name: ${name}`);showToast(t('saved'));tg.MainButton.hide();goBack();showProducts();}catch(err){showToast(err.message,true);}finally{tg.MainButton.hideProgress();tg.MainButton.enable();}}
export async function showEditProductForm(productId){tg.BackButton.show();tg.MainButton.setText(t('save'));tg.MainButton.show();tg.MainButton.onClick(()=>updateProduct(productId));const{data:product}=await supaCall(()=>getSupabase().from('products').select('*, variants(*)').eq('id',productId).single());document.getElementById('view').innerHTML=`<div class="card"><h3>تعديل ${sanitize(product.name)}</h3><div id="variants-container"></div><button class="btn btn-outline" id="add-variant-row">+ إضافة متغير</button></div>`;const container=document.getElementById('variants-container');product.variants.forEach(v=>{const row=document.createElement('div');row.className='variant-row';row.innerHTML=`<input value="${v.variant_name||''}" class="v-name"/><input type="number" value="${v.purchase_price}" class="v-purchase-price"/><input type="number" value="${v.selling_price}" class="v-selling-price"/><input type="number" value="${v.quantity}" class="v-qty"/><input type="number" value="${v.min_quantity}" class="v-min"/><button class="btn btn-sm btn-danger remove-variant">X</button>`;row.querySelector('.remove-variant').addEventListener('click',()=>row.remove());container.appendChild(row);});document.getElementById('add-variant-row').addEventListener('click',addVariantRow);}
async function updateProduct(productId){if(!confirm('سيتم استبدال جميع المتغيرات. متابعة؟'))return;const rows=document.querySelectorAll('.variant-row');const variants=[];rows.forEach(row=>{const vname=row.querySelector('.v-name').value.trim()||'بدون اسم';const purchasePrice=parseFloat(row.querySelector('.v-purchase-price').value);const sellingPrice=parseFloat(row.querySelector('.v-selling-price').value);const qty=parseInt(row.querySelector('.v-qty').value);const minQty=parseInt(row.querySelector('.v-min').value)||0;if(!vname||isNaN(purchasePrice)||isNaN(sellingPrice)||isNaN(qty))return;variants.push({variant_name:vname,purchase_price:purchasePrice,selling_price:sellingPrice,quantity:qty,min_quantity:minQty});});if(!variants.length){showToast(t('fillAllFields'),true);return;}tg.MainButton.disable();tg.MainButton.showProgress();try{await supaCall(()=>getSupabase().from('variants').delete().eq('product_id',productId));const newVariants=variants.map(v=>({...v,product_id:productId,user_id:getCurrentUserId()}));await supaCall(()=>getSupabase().from('variants').insert(newVariants));await logActivity('update_product',`ID: ${productId}`);showToast(t('saved'));tg.MainButton.hide();goBack();showProducts();}catch(err){showToast(err.message,true);}finally{tg.MainButton.hideProgress();tg.MainButton.enable();}}
