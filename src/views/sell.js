import { getSupabase, supaCall, logActivity, getCurrentUserId, recordCashTransaction } from '../utils/supabase-client.js';import { formatCurrency, showToast } from '../utils/helpers.js';import { t } from '../config/i18n.js';import { showInvoice } from './invoice.js';const tg=window.tg;let cart=[];export async function showSellForm(){window.currentRefreshFunction=showSellForm;tg.MainButton.setText('إتمام البيع');tg.MainButton.show();tg.MainButton.onClick(checkout);document.getElementById('view').innerHTML=`<div class="card"><h2>${t('sell')}</h2></div>`;async function checkout(){}}
